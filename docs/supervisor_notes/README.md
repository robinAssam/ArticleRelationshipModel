# Supervisor Notes

One file per meeting or shared note. Verbatim where possible — paraphrase
loses signal.


## File template

```markdown
# 2026-06-15 
We need to integrate more links so context based links between words etc.
Semantic simmilarity
Graph Nodes 
When inputting the system with an 'atom' it should be able to highlight 
where in the tree (of Law?) it most likley belongs to.
Idea : Give weights to attributes for example if one 'zone' x mentions 'property'
more often than another zone y, y should have less weight for 'property'

```

Files in this folder are referenced from `docs/03_decisions_log.md`
when a supervisor note triggers a project-level decision.
